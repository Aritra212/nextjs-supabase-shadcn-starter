# Supabase Configuration
# Get these values from your Supabase project dashboard (https://supabase.com/dashboard)
# Go to: Settings → API
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Your Supabase Anonymous (anon) public key
# This is safe to use in the browser
# Find it under: Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Your Supabase Service Role key (OPTIONAL)
# Only needed for server-side admin operations
# ⚠️ WARNING: Keep this secret! Never expose in client-side code or commit to Git
# Find it under: Project API keys → service_role
SUPABASE_SECRET_KEY=your_supabase_service_role_key

# Application Configuration
# Your app's URL - used for redirects and metadata
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_META_URL=http://localhost:3000
